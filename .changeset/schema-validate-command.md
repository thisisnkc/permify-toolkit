---
"@permify-toolkit/cli": minor
---

Add `schema validate` command — lint and validate schema syntax and references offline without pushing to Permify. Supports both TypeScript DSL schemas (full semantic validation) and `.perm` files (structural checks).
